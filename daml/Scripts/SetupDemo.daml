module Scripts.SetupDemo where

-- TODO (Story 1.3+): allocate demo parties, create seed LotPositions, and wire
-- the custody path (producer → truck → silo → railway → ports → vessel).
setupDemo : Script ()
setupDemo = script do
  pure ()
